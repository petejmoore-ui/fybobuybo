<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>ForYourBuysOnly</title>
<link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
<h1>ForYourBuysOnly</h1>
<div class="grid">
    {% for product in products %}
    <div class="card">
        <img src="{{ product.image }}" alt="{{ product.title }}">
        <h2>{{ product.title }}</h2>
        <a href="{{ product.url }}" target="_blank"><button>Grab It Now 🔥</button></a>
    </div>
    {% endfor %}
</div>
</body>
</html>
